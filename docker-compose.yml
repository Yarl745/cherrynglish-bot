version: "3.5"


services:
  bot:
    container_name: cherrynglishbot
    build:
      context: .
    command: python app.py
    restart: always
    networks:
      - cherrynet
    env_file:
      - ".env"
    volumes:
      - .:/src


networks:
  cherrynet:
    driver: bridge
